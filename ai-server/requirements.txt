# Runtime dependencies
fastapi==0.111.0
uvicorn[standard]==0.30.1
httpx==0.27.0
python-multipart==0.0.9
anyio==4.4.0

# Speech-to-text (heavy; downloads a model on first use).
faster-whisper==1.0.3
# faster-whisper fetches the model via huggingface_hub, which needs requests.
# Not always pulled in transitively, so pin it explicitly.
requests==2.32.3
