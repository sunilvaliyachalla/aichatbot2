import SwiftUI

/// App entry point (the iOS counterpart to Android's `MainActivity`).
@main
struct P2PCallApp: App {
    var body: some Scene {
        WindowGroup {
            AppRootView()
        }
    }
}
