# Port the signaling server listens on
PORT=4000

# Comma-separated list of allowed CORS origins for the web client.
# Use "*" for local development only. In production list explicit origins.
CORS_ORIGIN=*

# Max participants allowed per room. This starter targets 1:1 calls.
MAX_PEERS_PER_ROOM=2
